//WRITE YOUR CODE BELOW
let studentName = ['Robot ears', 'Curly Boys', 'Square Girls', 'No Mames guey']

for (let index in studentName) {
    console.log(`im ${studentName[index]}`);
}

studentName.forEach((student) => {
    console.log(`im ${student}`);
});